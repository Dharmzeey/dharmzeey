---
interface Props {
  id: string;
  class?: string;
}
const { id, class: className = "" } = Astro.props as Props;
---

<div class="h-full" id={id}>
  <div
    class={`modern-card h-full w-full p-6 m-auto mb-2 ${className}`}
  >
    <slot />
  </div>